import { Stack, Text, Title } from "@mantine/core";

export default function WfAnalysisPage() {
  return (
    <Stack gap="md">
      <Title order={3}>Walk-Forward Analysis</Title>
      <Text c="dimmed">WF analysis dashboards will appear here (mocked).</Text>
    </Stack>
  );
}
